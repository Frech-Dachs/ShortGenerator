# Frontend Service

React + Vite + TypeScript + Tailwind frontend for ShortGenerator.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

## Run with Docker from repo root

```bash
docker compose up --build frontend
```

The app is served on `http://localhost:5173`.
